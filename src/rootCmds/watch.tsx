import { Argv } from "yargs";

export const command = 'watch';

export const desc = 'Watch your Truffle build directory and automatically create Dapps for deployed contracts.';

export const builder = {}

export function handler(argv:Argv) {
  console.log('Stubbed out pending the rest of the API behaving')
}